# Security maintenance — 2026-09-25

This patch removes known vulnerable dependency versions while retaining the existing application and deployment architecture.

Added the secure AnyIO minimum and regenerated both hashed Python locks (resolved AnyIO 4.15.1). All 60 resolved Python package versions return no advisories; independent lock/constraint review passed.

## Verification

Frontend lint/build passed locally. All GitHub checks passed on the final head: Python 3.12/Postgres 16 tests/audit, frontend, containers, deployment-manifest validation and secret scan.

- `frontend`: npm audit 9 → 0 dependency entries; final high 0, critical 0. Counts are dependency entries, not unique CVEs.

Backend AnyIO 4.15.1 closes HTTP/2 denial-of-service advisories; requirements and the universal Python 3.12 hash lock agree. The 60-package resolved Python audit has zero findings. Python/Postgres integration, frontend, production-container and deployment-security CI checks passed on the source patch.

## Installation and rollback

Use the committed lockfile with the repository’s documented clean-install and validation commands. Deploy the merged revision through the existing release procedure; merging alone does not prove a running service uses the patch. Preserve the previous release and database backup for rollback. Do not reset a dirty local checkout or discard local-only commits.
